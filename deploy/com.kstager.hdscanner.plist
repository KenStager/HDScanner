<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.kstager.hdscanner</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>-lc</string>
    <string>export PYTHONUNBUFFERED=1; cd /Users/kstager/HDScanner &amp;&amp; . .venv/bin/activate &amp;&amp; hd run-once &amp;&amp; hd notify</string>
  </array>

  <key>StartCalendarInterval</key>
  <array>
    <dict><key>Hour</key><integer>0</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>4</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>8</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>12</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>16</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>20</integer><key>Minute</key><integer>0</integer></dict>
  </array>

  <key>WorkingDirectory</key>
  <string>/Users/kstager/HDScanner</string>

  <key>StandardOutPath</key>
  <string>/Users/kstager/HDScanner/hd_launchd.stdout.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/kstager/HDScanner/hd_launchd.stderr.log</string>

  <!-- Runs only on the calendar schedule above. launchd catches up a missed
       run when the Mac wakes, so RunAtLoad is not needed to avoid gaps and
       would otherwise fire an extra scan on every login. -->
  <key>RunAtLoad</key>
  <false/>
  <key>KeepAlive</key>
  <false/>
</dict>
</plist>
